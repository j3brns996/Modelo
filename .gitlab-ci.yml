# Activation requires a protected adapter and remote T10 rehearsal.
stages: [validate]

modelo/check:
  stage: validate
  script:
    - echo "GitLab trusted acceptance is not activated; complete the host-switch rehearsal." >&2
    - exit 1
