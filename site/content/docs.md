The human specification explains the system and its invariants. The machine contract and JSON Schemas are the executable reference.

Clone the repository and use the locked Python and uv toolchain. Node, npm and npx are not required.
