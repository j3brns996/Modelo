Modelo uses Git as the approval ledger. An issue records intent, a change request carries the proposed facts, and CI validates the exact head.

Approval requires the protected branch process. Provider availability is evidence, not enterprise approval. Evidence remains immutable and content-addressed.
